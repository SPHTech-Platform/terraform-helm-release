formatter: "markdown table"

#recursive:
#  enabled: true
#  path: modules

output:
  file: "README.md"
  mode: replace
  template: |
    # Terraform Helm Release

    {{ .Content }}

settings:
  lockfile: false
